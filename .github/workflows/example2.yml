name: Docker Image CI

on:
  push:
    branches: [ "feature2/test2" ]

env:
  EXAMPLE2: example22

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: step-nr-01
      run: |
        echo "Hello:  ${{ env.EXAMPLE2 }} ${{ env.EXAMPLE1 }}"
