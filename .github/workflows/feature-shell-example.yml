name: Docker Image CI

on:
  push:
    branches: [ "feature/test*" ]

env:
  DAY_OF_WEEK: Monday

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Hello world
      run: |
        echo "Hello World ${{ secrets.DEMO_DEV }}" ; ls -la ; pwd ; echo $DAY_OF_WEEK > test.txt ; cat test.txt
